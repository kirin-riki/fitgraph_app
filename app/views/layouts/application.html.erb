<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "FitGraph" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="standalone">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="FitGraph">
    <meta name="theme-color" content="#8B5CF6">
    <meta name="msapplication-TileColor" content="#8B5CF6">
    <meta name="mobile-web-app-capable" content="standalone">
    <meta name="application-name" content="FitGraph">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= favicon_link_tag "favicon.ico", rel: "icon" %>
    <%= favicon_link_tag "apple-touch-icon.png", rel: "apple-touch-icon", type: "image/png", sizes: "180x180" %>
    <%= favicon_link_tag "android-chrome-192x192.png", sizes: '192x192', type: 'image/png' %>

    <!-- PWAマニフェスト -->
    <link rel="manifest" href="/manifest.json">

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload", media: "all" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: "all" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module", defer: true %>
    <script src="https://cdn.jsdelivr.net/npm/chart.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns@3.0.0/dist/chartjs-adapter-date-fns.bundle.min.js" defer></script>

    <!-- OGP基本タグ（テスト用） -->
    <meta property="og:title" content="FitGraph - 体重管理アプリ">
    <meta property="og:description" content="毎日の体重や体調をグラフで記録・管理できるWebアプリです。">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://fitgraph-app.onrender.com/">
    <meta property="og:image" content="https://fitgraph-app.onrender.com/images/icon.png">

    <!-- Twitterカード用（テスト用） -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="FitGraph - 体重管理アプリ">
    <meta name="twitter:description" content="毎日の体重や体調をグラフで記録・管理できるWebアプリです。">
    <meta name="twitter:image" content="https://fitgraph.example.com/images/icon.png">
  </head>

  <body class="min-h-screen flex flex-col bg-gray-50">
    <%= render "shared/header" %>
    <main class="min-h-[calc(100vh-60px)] <%= content_for?(:no_main_padding) ? '' : 'pt-[60px]' %>">
      <%= yield %>
    </main>
    <% if user_signed_in? %>
      <%= render "shared/footer" %>
    <% end %>
    <script>
      document.addEventListener("DOMContentLoaded", () => {
        const btn  = document.getElementById("dropdown-button")
        const menu = document.getElementById("dropdown-menu")
        if (!btn || !menu) return
        btn.addEventListener("click", () => menu.classList.toggle("hidden"))
      })
    </script>
  </body>
</html>
